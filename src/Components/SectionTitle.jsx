const SectionTitle = ({ text }) => {
  return (
    <div>
      <h2 className="text-3xl font-light tracking-wider text-zinc-50 text-center">
        {text}
      </h2>
    </div>
  );
};
export default SectionTitle;
